# Merge this entry into the existing [autostart] section of
# ~/.config/wayfire.ini for the desktop user. Do not add a second [autostart].
[autostart]
adsb_vhf_kiosk = systemctl --user start adsb-kiosk.service
