# Installed as /etc/adsb-vhf/sdr.env. Single source for EEPROM serials.
# After rtl_eeprom -s, USB iSerial must match the SN: line from rtl_test -t
# (that is the librtlsdr serial used by --device / serial = "...").
ADSB_PREFERRED_SERIAL=1090
VHF_SERIAL=0118
AIRMON_RADIO_RECEIVER_SERIAL=0118
